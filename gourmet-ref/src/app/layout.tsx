import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";
import { ThemeProvider } from "@/components/theme-provider";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Gourmet Recipes - Culinary Excellence",
  description: "Discover gourmet recipes with detailed nutrition information from TheMealDB and USDA",
  keywords: ["recipes", "gourmet", "nutrition", "cooking", "TheMealDB", "USDA"],
  authors: [{ name: "Z.ai Team" }],
  icons: {
    icon: "https://z-cdn.chatglm.cn/z-ai/static/logo.svg",
  },
  openGraph: {
    title: "Gourmet Recipes",
    description: "Discover gourmet recipes with detailed nutrition information",
    url: "https://chat.z.ai",
    siteName: "Gourmet Recipes",
    type: "website",
  },
  twitter: {
    card: "summary_large_image",
    title: "Gourmet Recipes",
    description: "Discover gourmet recipes with detailed nutrition information",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        <ThemeProvider
          attribute="class"
          defaultTheme="dark"
          enableSystem
          disableTransitionOnChange
        >
          {children}
          <Toaster />
        </ThemeProvider>
      </body>
    </html>
  );
}
